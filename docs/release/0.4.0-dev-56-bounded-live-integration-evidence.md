# Lumi `0.4.0-dev-56` — bounded Live integration evidence

Dev-56 completes the software scope of Epic 5 and makes the remaining shared
engine handoff measurable. It does not weaken the requirement for the final
physical and one-hour release evidence.

## Delivered

- Pro DJ Link source age now includes both Java bridge queue residence and the
  Rust supervisor queue;
- a constant-space histogram reports p50, p95, p99, maximum and sample count;
- fresh Link clock observations leave the 5 ms engine coordinator before
  library hydration or lighting-plan reduction;
- the native Integration diagnostics expose source age alongside queue depth,
  high-water, coalescing and critical saturation;
- a single configurable runner exercises Pro DJ Link-only, Link-only,
  realtime MIDI-only and combined lanes;
- combined stress changes pitch, performs seeks, cycles lighting Pause/Start
  and polls snapshots at approximately 40 Hz;
- one bounded JSON artifact retains release evidence without track metadata,
  paths or credentials.

## Development evidence

The four 30-second development soaks passed against the representative
CDJ-1500X LAN simulator and real SoundSwitch Link session:

- Pro DJ Link-only: 661 messages, p95 5 ms, maximum 38.7 ms, queue high-water
  4/512 and zero critical saturation;
- Link-only: 948 received/applied clock changes, one hard acquisition, one
  SoundSwitch peer and zero failures;
- realtime MIDI-only: 1,024 emitted actions, 56 intentional cancellations,
  p95 10.029 ms and zero saturation;
- combined: 915 messages, source p95 10 ms and maximum 22.1 ms, 697 UI polls,
  14 pitch changes, four seeks, two lighting operation cycles, seven expected
  dry-run outputs, one Link peer and zero Link/output failures.

The complete functional and technical local gates pass. The installed native
app and SoundSwitch UI acceptance are recorded after packaging.

## Scope boundary

This is an unsigned Apple Silicon development build. The implementation work
for Epic 5 is complete, but RC promotion still requires the same runner at one
hour per lane over Wi-Fi and Ethernet plus physical CDJ-1500X, SoundSwitch,
Control One and visible DMX confirmation.
