# 0.3.0 – installable release readiness

Status: **Accepted for unsigned Stable owner release**
Datum: **2026-08-09**

## Release decision

The current product scope matches the planned `0.3.0 – SoundSwitch Live MVP`:
the real Rekordbox-backed library, Local Playback, phrase and AutoLoop planning,
automatic SoundSwitch MIDI execution and an initial physical DMX run exist.

The release branch now reports `0.3.0`. The selected version path is:

```text
integrated development baseline: 0.3.0-dev
accepted installable candidate: 0.3.0-rc.1
current production promotion: 0.3.0
next dev cycle: 0.4.0-dev-1
```

The canonical version was promoted from `0.3.0-dev` to `0.3.0-rc.1` only on
`release/v0.3.0`, following ADR-0009.

## Current branch and release state

Observed during promotion on 2026-08-09:

- `main` remains unchanged until the exact verified Stable tree is promoted;
- `dev` contains the complete tested `0.3.0-dev` product state;
- `release/v0.3.0` contains the accepted RC behavior plus final `0.3.0` version,
  license, notices and release metadata;
- no Developer ID/notarized packaging workflow exists yet;
- the macOS target is Apple Silicon-only and embeds `lumi-engine`, but its base
  configuration explicitly disables signing;
- the existing Release app is ad-hoc signed and deliberately distributed as an
  early owner/open-source build with explicit Gatekeeper instructions.

`main` must therefore not be fast-forwarded directly to the current feature
branch. The existing branch policy remains: feature PR to `dev`, release
preparation, then a deliberate `dev` to `main` release PR.

## Two distinct installable artefacts

1. **Local acceptance DMG** – useful for testing installation on the development
   Mac. It can be built without Apple distribution credentials, but Gatekeeper
   trust and clean-Mac distribution are not proven.
2. **Release DMG** – the real release candidate. The app and embedded engine are
   signed with Developer ID Application, Hardened Runtime is enabled, the DMG is
   notarized and stapled, and Gatekeeper accepts it on a clean Apple Silicon Mac.

The owner explicitly accepts the first artefact for the private `0.3.0`
release. A public release must continue to label it unsigned and provide the
EPL-2.0 source; Developer ID/notarization remains a later distribution upgrade.

## Required implementation slices

### R0 – Open-source publication decision

- publish under EPL-2.0 with compact branding and integration notices;
- publish supported macOS/hardware scope, privacy behavior, known limitations
  and contribution/security guidance;
- make clear that early unsigned builds require a one-time Gatekeeper override.

### R1 – Release configuration and version consistency

- use isolated Debug (`Lumi Dev`), Preview (`Lumi Preview`) and Release (`Lumi`)
  identities, preference domains and Application Support directories;
- set `VERSION`, Cargo and Apple product version to `0.3.0`, with the same
  numerical Apple marketing version;
- use a monotone `CFBundleVersion`;
- add release notes and known limitations for the RC;
- keep macOS 15 and `arm64` as the declared first-release platform.

### R2 – Deterministic app and DMG packaging

- build the Rust engine in release mode and embed it in `Contents/Helpers`;
- sign nested executables before signing the containing app;
- archive/export `Lumi.app` from a clean Release build;
- create `Lumi-0.3.0-arm64.dmg` with an Applications shortcut, EPL-2.0 license,
  source statement and notices;
- generate a SHA-256 checksum and a bounded SBOM;
- verify bundle identity, version, helper presence and launch before upload.

The first implementation produces a locally packaged, ad-hoc-signed DMG. It is
appropriate for owner and early open-source testing, but does not replace
Developer ID or notarization and must say so on the GitHub Release.

### R3 – Developer ID signing and notarization (deferred)

- provision an Apple Developer Program team and Developer ID Application
  certificate;
- store the certificate and notarization credentials outside the repository;
- submit with `xcrun notarytool`, wait for acceptance and staple the ticket;
- verify the app and DMG with `codesign`, `spctl` and `stapler validate`;
- install and launch the stapled DMG on a clean Apple Silicon test account or
  second Mac.

### R4 – Product acceptance gates

- accepted: repeat full-song SoundSwitch/Control One/DMX run after
  timing/lifecycle hardening;
- accepted: prove Live → Settings → Live playback and output continuity;
- defer the longer 100-trigger and endpoint reconnect soak to the next
  hardening cycle;
- record known limitations: no autonomous SMAppService yet, no production Pro
  DJ Link hardware evidence and no iPhone companion release;
- run the complete local verification gate from the exact RC commit.

### R5 – Controlled promotion to `main`

1. Squash/merge the active feature branch to `dev`.
2. Create `release/v0.3.0` from the updated `dev`.
3. Prepare `0.3.0-rc.1`, changelog, release notes and artefacts.
4. Perform installation and physical acceptance against that exact commit.
5. Set the final canonical version to `0.3.0`.
6. Merge `dev` to `main` using a normal release merge commit.
7. Create immutable tag `v0.3.0` on that `main` commit.
8. Build and ad-hoc sign again from the tag, verify the Stable DMG and publish
   the explicitly unsigned GitHub Release.
9. Merge `main` back to `dev` and set the next `-dev` version.

## Go/no-go

RC.1 produced a locally verified, ad-hoc-signed Lumi Preview DMG with a separate
bundle identity, preferences and database. On 2026-08-09 the owner accepted a
physical Local Playback → SoundSwitch → Control One → DMX run, including active
button feedback and Live → Settings → Live continuity. EPL-2.0 is selected.

The accepted minor follow-ups are intermittent pause → cue/seek → play output
reconciliation and explicit desired-versus-applied timing acknowledgement. They
are tracked in `story-e3-03-resume-and-timing-confirmation.md` for `0.4.0` and
do not block this deliberately unsigned private owner release. Developer ID and
notarization remain deferred until broader distribution justifies the paid
Apple program.
