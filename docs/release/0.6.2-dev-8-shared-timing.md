# Lumi 0.6.2-dev-8 — One saved timing setting

Remote offset changes now update the same permanent setting as Mac edits.
Remote 0.1.1-dev-4 remains compatible; no new iPhone binary is required.

- Channel-local engine persistence survives restart and does not require a UI
  observer to save changes.
- File I/O is isolated on a bounded background writer. Live integration paths do
  not wait for disk writes. Failed writes are visible in Mac Live, not reported
  as SAVED.
- Mac migrates its previous preference once and mirrors saved engine values
  without resending them. SESSION is removed.
- Changes during Start/playback still activate at the next phrase, without
  restarting the active AutoLoop.

## Acceptance

Verified 2026-09-05:

- Engine: 115 tests passed; nine existing lab tests ignored. Initial sandbox
  failures were local socket permission errors; the authorized rerun passed.
- Clippy: engine/all targets, warnings denied, passed.
- Mac Live: 58 tests passed, including saved desired timing distinct from the
  applied and pending values. Mac Xcode build and local package checks passed.
- Persistence tests cover ordered updates and reopen, channel isolation,
  invalid persisted values, failed write visibility/retry, and range rejection.
  The Remote command regression verifies that an accepted edit saves its desired
  value while activation is deferred; a conflicting edit cannot replace it.
- Installed dev-8 build 339 from `d62742d63996`. Native Mac UI confirmed migration
  of the existing −250 ms preference, change to −245 ms and SAVED, then a real
  app quit/relaunch retaining −245 ms. The channel file independently contained
  that value.
- Native Start/playback test restored −250 ms: UI showed −245 ms applied and
  −250 ms NEXT PHRASE while the persistent file already contained −250 ms, then
  showed −250 ms applied/SAVED after the next phrase. Left latest Mac Live open
  in Start with the original offset and Controller assignment unchanged.
- Remote dev-4 protocol/command behavior is unchanged. A fresh Remote native UI
  interaction could not be completed in this pass: Simulator returned invalid
  accessibility element IDs and no screenshot. Do not treat it as a new physical
  iPhone acceptance test. Engine Remote-command regression tests cover the new
  persistence path; no physical lighting latency measurement is claimed.

No library, phrase, MIDI mapping or pairing data was reset.
