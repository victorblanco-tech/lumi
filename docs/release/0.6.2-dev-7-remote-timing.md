# Lumi 0.6.2-dev-7 / Remote 0.1.1-dev-4 — Live timing edits

An offset selected from Remote could be rejected because the general engine
revision had advanced during playback. The next projection immediately cleared
the error. The original Menu also rebuilt with live updates and only offered
−100 to +100 ms, despite the engine supporting −250 to +250 ms.

Remote now has a stable timing sheet with the full range, a slider, 1 ms fine
adjustment, Reset, Cancel and Apply. A draft is not reset by incoming player
updates. Conflicts remain visible. NEXT PHRASE identifies a pending change;
Mac Live shows the engine's applied/pending value and identifies a session
override separately from its saved startup default.

The engine checks the expected timing value instead of unrelated show revisions
for new Remote clients. Concurrent offset changes conflict; legacy clients
retain their original revision contract. Playing shows defer activation until
the next phrase without retriggering the current AutoLoop.

## Verification

Verified on 2026-09-05:

- Rust engine, protocol and gateway: 161 tests passed, nine existing lab tests
  ignored. Clippy passed for the three crates, all targets, with warnings denied.
- Swift Remote client: 33 tests; Remote feature: 14 tests; Mac Live: 58 tests passed.
- Regression coverage includes full-range timing values, a stale general show
  revision with an unchanged timing value, genuine timing conflicts, legacy
  command validation, persistent rejection feedback and deferred phrase activation.
- Simulator and physical-device iOS builds succeeded. Mac package validation
  passed; installed Mac dev-7 build 337 is built from `e8b0207b9382`.
- Native UI reproduced the original live revision rejection, then verified the
  corrected command during a running two-player Pro DJ Link simulator show:
  Remote Reset/Apply to 0 ms produced NEXT PHRASE on both clients, followed by
  0 ms applied at the next phrase. The Mac distinguished SESSION from its saved
  default. Incoming playback updates did not reset the open editor's draft.
- Native 1 ms step adjustment and Cancel were checked: cancelling a +1 ms draft
  left the applied value at 0 ms. The full numeric range is covered in automated
  tests; a native slider drag across the full range was not verified.
- Restored the saved −250 ms offset and observed it applied in Mac Live. Kept
  the running show in Start. No physical lighting latency measurement is claimed.
- Remote dev-4 was installed in place on the USB-connected physical iPhone and
  launched. Mac pairing status confirmed its new version. Restored that iPhone
  as Controller and the Simulator as View only, without deleting pairings.
- Left the latest Mac app open. Existing user library and mapping data retained.

This targeted fix does not complete the remaining Epic 10 hardening work.
